services:
  backend:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: reconocimiento-backend
    networks:
      - node-network
    ports:
      - "5000:5000"

networks:
  node-network:
    driver: bridge
